apiVersion: v1
kind: ConfigMap
metadata:
  name: marketplace-config
  namespace: marketplace
data:
  ALLOWED_HOSTS: "marketplace.local,localhost,127.0.0.1,backend,frontend"
  CSRF_TRUSTED_ORIGINS: "http://marketplace.local,http://localhost,http://127.0.0.1"
  CORS_ALLOWED_ORIGINS: "http://marketplace.local,http://localhost,http://127.0.0.1"
