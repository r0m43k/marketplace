# Marketplace Frontend

React/Vite frontend for the Django API.

## Run

```bash
npm install
npm run dev
```

Copy `.env.example` to `.env.local` if the backend API is not on `http://127.0.0.1:8000/api`.
